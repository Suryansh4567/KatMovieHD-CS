version = 6

cloudstream {
    authors = listOf("arena")
    status = 1
    description = "Highest Quality UHD Movies and Series from OlaMovies. v5: direct-link minting — drive.olamovies.download/file/* pages are harvested for their embedded _id and rebuilt as instant olam.bypassbot stream URLs (reverse-engineered 2026-08-23). v4: playback fixed — v4-theme download links + generator-chain resolution."
    language = "hi"
    tvTypes = listOf("Movie", "TvSeries", "Anime")
    iconUrl = "https://www.google.com/s2/favicons?domain=v4.olamovies.mov&sz=64"
}
