version = 4

cloudstream {
    authors = listOf("arena")
    status = 1
    description = "Highest Quality UHD Movies and Series from OlaMovies. v4: playback fixed — download links are now read from the new v4 theme (links.ol-am.top generator buttons with release-name headers), generator pages are sniffed for stashed targets before the WebView ad-chain fallback, and the undefined getQualityFromName compile error is removed."
    language = "hi"
    tvTypes = listOf("Movie", "TvSeries", "Anime")
    iconUrl = "https://www.google.com/s2/favicons?domain=v4.olamovies.mov&sz=64"
}
